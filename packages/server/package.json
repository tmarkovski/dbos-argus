{
  "name": "@dbos-argus/server",
  "version": "0.0.1",
  "private": true,
  "description": "Turborepo task runner shim for the Python dbos-argus server package.",
  "scripts": {
    "lint": "uv run ruff check dbos_argus tests && uv run ruff format --check dbos_argus tests",
    "test": "uv run pytest tests",
    "build": "echo 'server build is handled via Docker / uv build'",
    "dev": "uv run --project ../.. uvicorn dbos_argus.main:app --host 0.0.0.0 --port 8090 --reload --reload-dir dbos_argus"
  }
}
